# Зависимости для анализаторов логов атак
# Все необходимые библиотеки являются частью стандартной библиотеки Python 3.6+

# Основные зависимости (встроенные в Python)
# re - для регулярных выражений
# sys - для системных функций
# collections - для специализированных контейнеров данных
# datetime - для работы с датами и временем
# argparse - для парсинга аргументов командной строки

# Дополнительные библиотеки (опционально)
# colorama>=0.4.6 - для цветного вывода в терминале (Windows)
# rich>=13.0.0 - для красивого форматирования вывода
# pandas>=1.5.0 - для расширенного анализа данных
# matplotlib>=3.6.0 - для создания графиков и диаграмм

# Для расширенного анализа (опционально)
# geoip2>=4.7.0 - для геолокации IP-адресов
# requests>=2.28.0 - для HTTP-запросов к внешним API
# beautifulsoup4>=4.11.0 - для парсинга HTML

# Для машинного обучения (опционально)
# scikit-learn>=1.1.0 - для кластеризации IP-адресов
# numpy>=1.21.0 - для численных вычислений

# Для веб-интерфейса (опционально)
# flask>=2.2.0 - для создания веб-интерфейса
# dash>=2.6.0 - для интерактивных дашбордов

# Примечание: Основные скрипты работают без установки дополнительных зависимостей
# Установите только те библиотеки, которые вам нужны для расширенного функционала
