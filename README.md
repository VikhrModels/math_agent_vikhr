# Анализатор логов атак

Набор инструментов для автоматического анализа логов веб-серверов на предмет выявления атак и подозрительной активности.

## Возможности

- 🔍 **Автоматическое выявление атак** различных типов
- 📊 **Детальная статистика** по IP-адресам и типам атак
- ⏰ **Временная линия** атак
- 🎯 **Специализированный анализ** SQL-инъекций
- 📝 **Автоматическая генерация отчетов** в Markdown

## Типы выявляемых атак

### SQL-инъекции
- UNION-based атаки
- Boolean-based атаки
- Time-based атаки
- Error-based атаки
- Stacked queries
- Information gathering
- System commands

### XSS (Cross-Site Scripting)
- Script tags
- JavaScript events
- DOM manipulation
- Cookie theft

### Path Traversal
- Directory traversal
- File inclusion
- Local file inclusion
- Remote file inclusion

### Другие атаки
- Command injection
- Directory listing
- Information disclosure

## Установка

```bash
# Клонирование репозитория
git clone <repository-url>
cd math_agent_vikhr

# Установка зависимостей
pip install -r requirements.txt
```

## Использование

### 1. Общий анализ логов

```bash
python3 log_analyzer.py access.log -o report.md -v
```

**Параметры:**
- `access.log` - путь к файлу логов
- `-o report.md` - файл для сохранения отчета
- `-v` - подробный вывод

### 2. Специализированный анализ SQL-инъекций

```bash
python3 sql_injection_analyzer.py access.log -o sql_report.md -v
```

**Параметры:**
- `access.log` - путь к файлу логов
- `-o sql_report.md` - файл для сохранения отчета
- `-v` - подробный вывод с анализом последовательностей

### 3. Анализ конкретного файла

```bash
# Анализ access_v2.log
python3 log_analyzer.py /path/to/access_v2.log -o attack_report.md -v

# Анализ на предмет SQL-инъекций
python3 sql_injection_analyzer.py /path/to/access_v2.log -o sql_injection_report.md -v
```

## Примеры использования

### Анализ логов Apache/Nginx

```bash
# Стандартный анализ
python3 log_analyzer.py /var/log/apache2/access.log -o apache_analysis.md

# Анализ SQL-инъекций
python3 sql_injection_analyzer.py /var/log/nginx/access.log -o nginx_sql_analysis.md
```

### Анализ больших файлов

```bash
# Для больших файлов (более 100MB)
python3 log_analyzer.py large_access.log -o large_analysis.md

# Прогресс обработки отображается каждые 10,000 строк
```

## Формат логов

Инструменты поддерживают стандартный формат логов Apache/Nginx:

```
IP - - [timestamp] "METHOD /path HTTP/version" status_code bytes_sent
```

**Пример:**
```
192.168.1.1 - - [25/Jul/2019:10:30:45 +0700] "GET /admin/login HTTP/1.1" 200 1234
```

## Структура отчетов

### Общий отчет (log_analyzer.py)
- Общая статистика атак
- Статистика по типам атак
- Статистика по IP-адресам
- Временная линия атак
- Примеры атак

### Отчет по SQL-инъекциям (sql_injection_analyzer.py)
- Детальный анализ SQL-атак
- Анализ последовательностей символов
- Возможные слова в последовательностях
- Структурные совпадения

## Конфигурация

### Настройка паттернов атак

Отредактируйте файлы `log_analyzer.py` и `sql_injection_analyzer.py` для добавления новых паттернов:

```python
self.patterns = {
    'new_attack_type': [
        r"pattern1",
        r"pattern2"
    ]
}
```

### Настройка регулярных выражений

```python
# Пример добавления нового паттерна
r"new_pattern\s*=\s*[^\s]+"
```

## Примеры отчетов

### Обнаруженные атаки

```
# Отчет об анализе логов атак

## Общая статистика
- Всего обнаружено атак: 6
- Подозрительных IP-адресов: 2
- Типов атак: 3

## Статистика по типам атак
### FILE_INCLUSION
- Количество: 4
- Топ IP-адресов:
  - 136.92.43.74: 4 атак
```

### SQL-инъекции

```
## Анализ последовательностей символов
### 36.32.3.74
#### 2019-07-24 11:32
- `Y` -> `/bkd_baru/dosen?input=Y`
- `A` -> `/bkd_baru/dosen?input=A`
- `N` -> `/bkd_baru/dosen?input=N`
```

## Требования

- Python 3.6+
- Стандартные библиотеки Python (re, sys, collections, datetime, argparse)

## Лицензия

[Указать лицензию]

## Поддержка

Для вопросов и предложений создавайте issues в репозитории.

## Вклад в проект

Приветствуются pull requests для улучшения функциональности и исправления ошибок.

## Безопасность

⚠️ **Важно:** Эти инструменты предназначены для анализа логов в целях безопасности. Не используйте их для атак или несанкционированного доступа к системам.

## Примеры реальных атак

### Path Traversal
```
GET /vulnerable.php?file=../../../etc/passwd HTTP/1.1
```

### SQL-инъекция
```
POST /login.php HTTP/1.1
username=admin' OR '1'='1&password=test
```

### XSS
```
GET /search.php?q=<script>alert('XSS')</script> HTTP/1.1
```

## Рекомендации по безопасности

1. **Регулярно анализируйте логи** на предмет подозрительной активности
2. **Настройте мониторинг** в реальном времени
3. **Блокируйте подозрительные IP-адреса**
4. **Обновляйте ПО** и зависимости
5. **Используйте WAF** для дополнительной защиты


